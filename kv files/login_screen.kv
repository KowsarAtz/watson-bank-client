#:import utils kivy.utils
<LoginScreen>:
    canvas:
        Color:
            rgb: utils.get_color_from_hex("#0c1632") 
        Rectangle:
            size: self.size
            pos: self.pos
    orientation: 'vertical'
    spacing: '10'
    BoxLayout:
        size_hint: (1,7)
    TextInput:
        id: usernameTextInput
        write_tab: False
        multiline: False
        hint_text: 'Username'
        size_hint: (0.3, 1)
        pos_hint: {"center_x":0.5}
    TextInput:
        id: passwordTextInput
        write_tab: False
        multiline: False
        password: True
        hint_text: 'Password'
        size_hint: (0.3, 1)
        pos_hint: {"center_x":0.5}
    Button:
        text: 'LOGIN'
        size_hint: (0.3, 1)
        pos_hint: {"center_x":0.5}
        on_release: root.loginCallback()
    BoxLayout:
        size_hint: (1,7)